# v0.8 - 幾何 (Geometry)

## 目標

實作 2D、3D 及 nD 幾何運算，包括向量、點、直線、平面、圓、球體等基本幾何物件。

## 參考來源

- `/Users/Shared/ccc/project/math4py/src/math4py/geometry/`
- https://github.com/ccc-js/j6/blob/master/lib/geometry.js

## 目錄結構

```
src/math4js/geometry/
├── index.ts           # 匯出
├── point.ts          # 點 (Point1D~PointND)
├── vector.ts         # 幾何向量 (Vec2, Vec3, Vec)
├── line.ts           # 直線 / 射線 / 線段
├── plane.ts          # 平面
├── circle.ts         # 圓 / 弧
├── sphere.ts         # 球面 / 弧面
├── polygon.ts        # 多邊形 (2D)
├── polyhedron.ts     # 多面體 (3D)
├── distance.ts       # 距離計算
└── transform.ts      # 幾何變換 (旋轉、平移、縮放)
test/
  geometry/
    test_point.ts
    test_vector.ts
    test_line.ts
    test_plane.ts
    test_circle.ts
    test_sphere.ts
    test_polygon.ts
    test_transform.ts
```

## 功能項目

### 1. 點 (point.ts)

- [ ] `Point1D(x)`, `Point2D(x, y)`, `Point3D(x, y, z)`
- [ ] `PointND(coords)` - 任意維度
- [ ] `distance(p)` - 兩點距離
- [ ] `midpoint(p)` - 中點
- [ ] `translate(v)` - 平移
- [ ] `scale(factor)` - 縮放

### 2. 向量 (vector.ts)

- [ ] `Vec2(x, y)`, `Vec3(x, y, z)`, `Vec(dims)`
- [ ] `add`, `sub`, `mul` - 向量運算
- [ ] `dot(v)`, `cross(v)` - 內積/外積
- [ ] `normalize()` - 單位向量
- [ ] `length()` - 向量長度
- [ ] `project(v)` - 投影
- [ ] `reflect(n)` - 反射

### 3. 直線 (line.ts)

- [ ] `Line2D(p1, p2)` - 2D 直線
- [ ] `Line3D(p1, p2)` - 3D 直線
- [ ] `Ray(p, direction)` - 射線
- [ ] `Segment(p1, p2)` - 線段
- [ ] `containsPoint(p)` - 點是否在線上
- [ ] `distanceTo(p)` - 到點的距離
- [ ] `intersect(line)` - 與另一線相交

### 4. 平面 (plane.ts)

- [ ] `Plane(normal, point)` - 法向量 + 點
- [ ] `Plane.fromPoints(p1, p2, p3)` - 三點建立
- [ ] `distanceTo(point)` - 到點距離
- [ ] `containsPoint(p)` - 點是否在平面上
- [ ] `intersect(line)` - 與直線相交
- [ ] `project(point)` - 投影點到平面

### 5. 圓 (circle.ts)

- [ ] `Circle(center, radius)` - 圓
- [ ] `Arc(center, radius, start, end)` - 弧
- [ ] `area()`, `circumference()` - 面積/周長
- [ ] `containsPoint(p)` - 內部檢測
- [ ] `tangent(point)` - 切線

### 6. 球面 (sphere.ts)

- [ ] `Sphere(center, radius)` - 球面
- [ ] `SphereSection(center, radius, theta1, theta2)` - 球面部分
- [ ] `area()`, `volume()` - 表面積/體積
- [ ] `containsPoint(p)` - 內部檢測
- [ ] `intersect(ray)` - 與射線相交

### 7. 多邊形 (polygon.ts)

- [ ] `Polygon(vertices)` - 任意頂點
- [ ] `Triangle`, `Rectangle`, `RegularPolygon`
- [ ] `area()` - 面積
- [ ] `perimeter()` - 周長
- [ ] `centroid()` - 重心
- [ ] `containsPoint(p)` - 點是否在多邊形內

### 8. 多面體 (polyhedron.ts)

- [ ] `Tetrahedron`, `Cube`, `Octahedron`
- [ ] `Polyhedron(vertices, faces)` - 一般多面體
- [ ] `volume()` - 體積
- [ ] `surfaceArea()` - 表面積
- [ ] `centroid()` - 重心

### 9. 距離計算 (distance.ts)

- [ ] `distance(p1, p2)` - 點到點
- [ ] `pointToLine(p, line)` - 點到線
- [ ] `pointToPlane(p, plane)` - 點到平面
- [ ] `pointToCircle(p, circle)` - 點到圓
- [ ] `pointToSphere(p, sphere)` - 點到球面

### 10. 幾何變換 (transform.ts)

- [ ] `translate(v)` - 平移
- [ ] `rotate2D(angle)` - 2D 旋轉
- [ ] `rotate3D(axis, angle)` - 3D 旋轉
- [ ] `scale(factor)` - 縮放
- [ ] `reflect(axis)` - 反射
- [ ] `transform(matrix)` - 一般矩陣變換

## API 設計

```typescript
import {
  Point2D, Vec2, Line2D, Circle,
  distance, translate, rotate2D
} from 'math4js/geometry';

const p1 = new Point2D(0, 0);
const p2 = new Point2D(3, 4);
distance(p1, p2); // 5

const c = new Circle(new Point2D(0, 0), 1);
c.area(); // π

const v = new Vec2(1, 0);
rotate2D(v, Math.PI / 2); // Vec2(0, 1)
```

## 依賴

- `ndarray` - 向量/矩陣運算
- `linear_algebra/Matrix` - 線性變換

## 預定產出

- `src/math4js/geometry/` 模組
- `test/geometry/` 測試
- 對應理論文件

## 預計時間

4-5 小時